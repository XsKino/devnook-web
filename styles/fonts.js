/* eslint-disable camelcase */
import { Quicksand, Fugaz_One } from 'next/font/google'

export const quicksand = Quicksand({
  subsets: ['latin']
})

export const fugazOne = Fugaz_One({
  weight: '400',
  subsets: ['latin']
})
